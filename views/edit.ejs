<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Habit</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Habit Tracker</h1>
  </header>
  
  <div class="container">
    <h2>Edit Habit</h2>
    <form action="/habits/<%= habit._id %>?_method=PUT" method="POST" class="habit-form">
      <label for="name">Habit Name</label>
      <input type="text" id="name" name="habit[name]" value="<%= habit.name %>" required>
      
      <label for="status">Status for Today</label>
      <select id="status" name="habit[status]">
        <option value="Done" <%= habit.status === 'Done' ? 'selected' : '' %>>Done</option>
        <option value="Not Done" <%= habit.status === 'Not Done' ? 'selected' : '' %>>Not Done</option>
        <option value="None" <%= habit.status === 'None' ? 'selected' : '' %>>None</option>
      </select>
      
      <button type="submit">Update Habit</button>
    </form>
    <a href="/habits" class="cancel-button">Cancel</a>
  </div>
</body>
</html>
